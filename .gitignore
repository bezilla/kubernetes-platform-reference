# Everything `make up` generates. Nothing here is a source of truth.
/.work/

# Local kubeconfig exports
/kubeconfig

# Written by CI and by the failure capture. Never a source of truth.
/.ci-artifacts/
